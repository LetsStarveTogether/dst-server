base_dir: "/data/netdata/log/otel"

endpoint:
  path: "10.255.255.254:4317"

logs:
  rotation:
    default:
      max_file_size: "100MB"
      max_entries: 200000
  retention:
    default:
      max_files: 500000
      max_total_size: "1TB"
      max_age: "9 years"
